import { cityPopularity } from 'super-rentals/helpers/city-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | city popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cityPopularity([42]);
  assert.ok(result);
});
