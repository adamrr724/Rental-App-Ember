import { rentalCost } from 'super-rentals/helpers/rental-cost';
import { module, test } from 'qunit';

module('Unit | Helper | rental cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalCost([42]);
  assert.ok(result);
});
